{% extends 'master/index.html.twig' %}

{% block content %}

    {% include 'partials/page-title.html.twig' with {'color': 'gray' } %}

    <div class="sub-services d-none d-md-block">
        {% for subService in entry.subServices %}
            <a href="#sub-service-{{ subService.id }}" class="sub-services__link">{{ subService.subServiceTitle }}</a>
        {% endfor %}
    </div>

    <div class="container">
        <div class="row justify-content-end justify-content-xxl-center">
            <div class="col-xs-24 col-md-18 col-md-12">
                {% include 'partials/text.html.twig' %}
            </div>
        </div>
    </div>

{% endblock %}